node_modules/

# Next.js generated output
.next/
out/

# Environment / secrets
.env
.env.local
.env*.local

# Playwright
test-results/
playwright-report/

# Local/editor/system artifacts
.DS_Store
*.tsbuildinfo
.agents/
.claude/
.cursor/
.jules/
.antigravitycli/
.worktrees/
.gemini/

# Python bytecode
__pycache__/
*.pyc

# Supabase local state (contains project ref / org IDs)
supabase/.temp/

# Pipeline runtime data (scraping state, repair reports, logs)
pipeline/data/*
# Build input: src/lib/clubs.ts imports it, so the app can't build without it
!pipeline/data/account_activity.json
