# Collection run audit — September 21, 2026

Run: [35663009527, attempt 2](https://github.com/Highlander-Builders-Initiative/HighlanderHub/actions/runs/35663009527/attempts/2).
The latest rerun executed at 23:06–23:27 UTC on commit `66fc540` and failed after 1,265.4 seconds.
Evidence: attempt-2 job logs and diagnostics artifact, plus read-only Supabase snapshots of the eight affected sources and their extraction caches.

## Why it failed

Two `ucrextension` first-slide downloads failed with `Network is unreachable`:

- `3989132051631020922`
- `3991125671749129369`

Both use `instagram.ftpa1-1.fna.fbcdn.net`. A live DNS check returned only an IPv6 address, and direct downloads also timed out locally. The same signed paths and query strings returned HTTP 200 JPEG images through `scontent.cdninstagram.com`.

The extraction stage correctly failed while retaining completed work. Publication then correctly failed for those same two incomplete sources. Neither source had event IDs in its current source-assessment record.

## Other results

- Collection completed with no incomplete Apify results reported by its batches.
- Extraction: 1,708 posts, 1,630 cache hits, 76 new extractions, two errors.
- Assessment: 159 model calls reported, 1,327 cache hits, 158 past sources skipped, 60 cached refusals skipped, six new refusals.
- Publication reported 161 rows written and zero deleted. This is a write count, not a count of newly discovered events.
- Reconciliation reported zero canonical updates and 18 duplicate/deleted rows removed.
- Zero Discord notifications were sent.
- One Gemini 504 recovered through the SDK retry and did not cause the final failure.
- Node deprecation and automatic-function-calling warnings were nonfatal.

Attempt 1 had different failures: missing `reserve_vision_ocr_request` and Vertex `aiplatform.endpoints.predict` permission denial. Those errors did not recur in attempt 2; do not confuse its older diagnostics artifact with this rerun.

## Repairs and verification

The downloader now retries connection/timeouts up to three attempts. An unreachable regional Instagram CDN can fall back to the general Instagram CDN while preserving the entire signed path and query. Expired HTTP responses still fail, and unrelated hosts are never rerouted.

The patched downloader fetched both exact failed URLs successfully. Returned bytes matched the independently downloaded fallback images (177,022 and 122,381 bytes).

Three parser gaps were reproduced against saved model responses and corrected:

| Source | Rejected evidence | Result |
| --- | --- | --- |
| `3990601324940617261` — AMWA | `tomorrow at 11a` | Recognizes abbreviated meridiem with token boundaries. |
| `3989136644019773547` — water polo | `28th of September` | Recognizes day-of-month wording and preserves year checks. |
| `3991245831017367319` — CHASS | `Mon, Sept 28 – Wed, Sept 30` | Recognizes interior days of a short range with consistent weekday/date endpoints. |

Both saved attempts for each of these three sources pass offline validation after the fixes. That does not establish every semantic choice: water polo's `8:00–10:00` still lacks an explicit AM/PM label, and CHASS remains subject to the existing one-event-per-post publication policy.

Three other new refusals remain unchanged:

- `3989785973017299564`: Cupid caption says before `10/7`, while the slide says before `10.6`; the model chose October 6 at 23:59:59. Review the conflicting cutoff evidence before publishing.
- `3991146438212207469`: screening response chose 19:00 from “Doors are at 7”; the caption explicitly labels the screening itself as 8pm. A corrected response can use the supported screening time.
- `3990631994090066591`: ice-cream social says “Wednesday”; resolving an unqualified weekday from posting context is not currently supported by the conservative date validator.

Validation: all 511 Python pipeline tests passed, including new transport and date/time regressions. `git diff --check` passed.

## Activation and remaining recovery

Changes are local and have not been pushed or run in Actions. No production rows, extraction caches, or assessment decisions were modified during this audit.

After deployment, ordinary extraction retries the two download failures and reuses completed OCR. Cached grounding refusals do not automatically refresh when parser code changes. Any recovery of those records must explicitly target the source IDs, review a dry run, and then apply only supported results. No archive-wide reassessment is needed.
