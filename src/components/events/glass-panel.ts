/**
 * Frosted side-panel surface for the /events rails. Translucent white over the
 * page's tonal-step background, a hairline ink edge, generous rounding, and the
 * resting card shadow. Shared so the left (Browse) and right (Calendar) rails
 * read as one material.
 */
export const GLASS_PANEL_CLASS =
  "rounded-[20px] border border-ink/[0.06] bg-white/70 p-4 shadow-card backdrop-blur-xl";
